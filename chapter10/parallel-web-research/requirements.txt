# Core mechanisms (message bus / parallelism / cascading termination / race conditions) use only Python standard library asyncio, no third-party dependencies required.
# The following dependencies are only used for "optional real LLM judgment" and reading keys from .env; the offline demo works without them.
openai>=1.30.0
python-dotenv>=1.0.0
