# Experiment 10-8 Dependencies
openai>=1.40.0
python-dotenv>=1.0.0
# Voice (--voice) uses OpenAI tts-1, no additional dependencies required;
# On macOS, --play playback uses the system's built-in afplay.
