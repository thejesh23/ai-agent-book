# Copy as .env or export directly to environment variables
# Primary: OpenAI; if OPENAI_API_KEY is not set, fall back to OPENROUTER_API_KEY (general fallback)
OPENAI_API_KEY=sk-your-openai-key
# Optional: override the default model (default: gpt-5.6-luna)
OPENAI_MODEL=gpt-5.6-luna
