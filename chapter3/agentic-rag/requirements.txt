openai>=1.0.0
requests>=2.31.0
python-dotenv>=1.0.0
# Optional: Chinese word segmentation for offline BM25 backend (offline_retriever.py / compare_offline.py).
# If not installed, it will automatically fall back to character n-gram segmentation; functionality is unaffected, only recall is slightly reduced.
jieba>=0.42.1
