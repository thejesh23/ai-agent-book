openai>=1.40.0
python-dotenv>=1.0.0
# System-level ffmpeg / ffprobe is also required (for generating silence, concatenating, and verifying audio):
#   macOS:  brew install ffmpeg
#   Ubuntu: apt-get install ffmpeg
