# Experiment 8-6: Designing an Evaluation Dataset for a Self-Evolving Agent
# Only the harness / demo itself requires these two dependencies; the domain-specific libraries mentioned in dataset.json
# (youtube-transcript-api, yfinance, etc.) are merely "reference solutions" – the Agent under test will install them, the evaluator does not need them.
openai>=1.30.0
python-dotenv>=1.0.0
