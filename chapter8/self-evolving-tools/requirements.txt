openai>=1.40.0
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
# Note: Domain-specific libraries such as yfinance are intentionally omitted here.
# The core of this experiment is to let the Agent discover them online and pip install them on demand in the sandbox.
