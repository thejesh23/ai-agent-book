# Dependencies required for running the backend standalone (a subset consistent with root requirements.txt)
fastapi>=0.110
uvicorn[standard]>=0.27
# The following are only needed when using `--model` to enable real LLM conversation mode; the default echo mode does not require installation:
# openai>=1.30
# python-dotenv>=1.0
